---
import GithubLogo from "@/assets/github.svg";
import LinkedinLogo from "@/assets/linkedin.svg";

const navItems = [
	{ title: "Home", path: "/" },
	{ title: "About", path: "/about" },
	{ title: "Posts", path: "/posts" },
];
---

<style>
    @media (min-width: 48rem) {
        .sidebar {
            width: 14rem;
            padding: 1.5rem;
            font-size: 0.7rem;
        }
        .title h1 {
            font-size: 2.2rem;
        }
        .about {
            font-size: 0.9rem;
        }
        .nav {
            font-size: 0.55rem;
            gap: 0.1rem;
        }
        .logo {
            width: 12px;
        }
    }
    @media (min-width: 72rem) {
        .sidebar {
            height: 100%;
            width: 20rem;
            padding: 2rem;
            font-size: 1rem;
        }
        .title h1 {
            font-size: 3rem;
            margin-bottom: 0.5rem;
        }
        .about {
            font-size: 1.15rem;
        }
        .nav {
            font-size: 0.75rem;
            gap: 0.2rem;
        }
    }
    .sidebar {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        color: #9a9a9a;
        background-color: #0b040f;
    }

    .sidebar a {
        color: #ddd;
        text-decoration: none;
    }

    .sidebar a:hover {
        color: #fff;
    }

    .title a {
        color: #ddd;
        text-decoration: underline purple 0.3rem;
        text-underline-offset: 0.2rem;
    }

    .about {
        line-height: 1.8;
        font-weight: 300;
        color: #bbb;
    }

    .about hr {
        margin: 0.7rem 0;
        position: relative;
        border: 0;
        border-top: 0.2rem solid purple;
    }

    .nav {
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .nav a {
        display: inline-flex;
        align-items: center;
    }
    .logo {
        opacity: 70%;
    }
    .logo:hover {
        opacity: 100%;
    }
    .nav-item-active {
        font-weight: 700;
    }
</style>
<div class="sidebar">
    <div class="title">
        <a href="/"><h1>yonathan <br />gashu</h1></a>
    </div>
    <div class="about">
        <p>
            i like computers and cats and games and music and drawing and
            snowboaridn gand skateboaridng and i like to play bass and guitar
            too!
        </p>
        <hr />
        <nav class="nav">
            {
                navItems.map(({ title, path }) => (
                    <>
                        <a
                            href={path}
                            class:list={{
                                ["nav-item-active"]:
                                    Astro.url.pathname === path,
                            }}
                        >
                            {title}
                        </a>
                        ✦
                    </>
                ))
            }

            <a href="https://yonathangashu.github.io/resume/resume.pdf"
                >Resume</a
            >
            ✦
            <a href="https://github.com/YonathanGashu"
                ><GithubLogo class="logo" width="1rem" height="auto" /></a
            >
            ✦
            <a href="https://linkedin.com/in/ygashu"
                ><LinkedinLogo class="logo" width=".9rem" height="auto" /></a
            >
        </nav>
    </div>
</div>
